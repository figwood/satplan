apiVersion: v1
kind: Service
metadata:
  name: {{ include "satplan.fullname" . }}
  labels:
    {{- include "satplan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "satplan.selectorLabels" . | nindent 4 }}
